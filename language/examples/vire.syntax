# Example: user-defined keyword spellings.
# Format: canonical = custom_spelling   (# = comment)
# Place next to a .vr file; `vire build/run` loads it automatically.
fn       = funktion
return   = gib
while    = solange
for      = fuer
mut      = veraenderlich
if       = wenn
else     = sonst
