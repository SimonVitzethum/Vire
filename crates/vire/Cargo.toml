[package]
name = "vire"
edition = "2021"
version = "0.1.0"

[[bin]]
name = "vire"
path = "src/main.rs"

[dependencies]
fastllvm-ir = { path = "../ir" }
fastllvm-solver = { path = "../solver" }
fastllvm-backend = { path = "../backend" }
