{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "vire",
            "request": "launch",
            "name": "Vire: Debug current .vr file",
            "program": "${file}",
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}
